# 更新日志

## V9.5.11 · 2026-09-16 · 背包格子名字再放大到 13px

父亲大人：还能再大点，两行没关系。

- `.bg-slot .bg-name`：11px → **13px**（行高 1.25，两行 = 32px，两行以内完整显示、超出才截断）
- `.bg-slot .bg-count`：11px → **12px**，跟名字更协调

## V9.5.10 · 2026-09-16 · 修「深井卡的终局挑战标签跟标题没对齐、还跟下一行贴住」

父亲大人指出的根因：`.t1` / `.t2` 的样式只写成 `.list-row .t1` / `.list-row .t2`，
而残域的世界卡、深井卡、队伍卡**不在 .list-row 里** → 吃不到这两条规则：
- 「终局挑战」标签按文字基线挂下去，比「深井」低一截；
- 下面那行没有 `margin-top`，直接贴着标签底边。

修法：`.t1` / `.t2` 补一套**通用规则**（和 .list-row 里那套一致）：
标题行 `display:flex; align-items:center; gap:6px`，标签 `align-self:center; line-height:1.4`，
副标题 `margin-top:3px`。任何卡片里的标题/副标题从此长相一致。

## V9.5.9 · 2026-09-16 · 背包格子里的名字放大、允许两行

父亲大人：格子里的道具 / 材料 / 装备名跟数量一样大，看不清。

- `.bg-slot .bg-name`：9px → **11px 加粗**，`line-height:1.2`、`max-height:27px`
  （最多两行，超了两行再截断），格子抬高到 62px 装得下两行。
- `.bg-slot .bg-count` 10px → 11px；`.bg-sub` 9px → 10px。

## V9.5.8 · 2026-09-16 · 细节四连：箭头贴右 / 阵型对齐 / 再删两处过度解释

父亲大人指出的四处，连同同类问题：

- **残域列表与深井卡的「›」箭头**：以前跟在文字后面（长短不一、还偏上）→ 新增 `.chev`：
  `margin-left:auto + align-self:center`，一律贴卡片右内边、垂直居中。
- **阵型行里说明与加成压在一起**（"同一阵营 5 人（主角可补位）"顶到"攻+14%"上）：
  `.fm-req` 改成 `flex:1 1 auto; min-width:0`（长说明先折行），`.fm-buff` 钉右且 `nowrap`。
- **删掉两处过度解释**：「还没有伙伴上阵。上阵共 5 格（前 2 后 3），主角占 1 格，还能再上 4 名队友。」
  → 只留「还没有伙伴上阵。」；阵型标题右边的「主角是"万能补位"」→ 删。
- 阵型卡底部那段「「同阵营」一族只取命中的最高档…」→ 删（属于解释）。

`test_ui.js` 里那条拿"万能补位"当锚点的断言，改成断行为（阵型列表在）。

## V9.5.7 · 2026-09-16 · 战力挪到卡片右上角

父亲大人：主角/伙伴卡里的「战力 1219」应该钉在右上角，别卡在名字旁边。

- 原因：那一行用的是 `class="grow"`，而 `.grow { flex: 1 }` 只写在 `.list-row .grow` 里——
  在别的容器里它根本不撑开，战力就贴在名字后面了。
- 改法：一行三块（头像 / 中间信息 / 战力），中间那块给 `flex:1;min-width:0` 自己撑开，
  整行 `align-items:flex-start`，战力钉在**右上角**。主角卡与伙伴卡同时改。

## V9.5.6 · 2026-09-16 · 全站过一遍"过度注释"：只留数与短标签

父亲大人定的标准：**可以留简短的数值标签**（例：`EXP 82% · 当前挂机 14.9 EXP/分`），
凡是"讲道理 / 教你怎么点"的句子一律删。

这一版扫了全站 60 多处小字（`.hint` / `.note`），压缩或删除 47 处，例如：

- 六维 / 技能 / 装备 / 等级条那几句操作说明 → 删；等级条只留 `EXP 82% · 当前挂机 14.9 EXP/分`
- 药园：「花◈点数种下灵田，到点回来收…不用一直刷副本」→ 只留「有几率出稀有物（兽魂石 / 装备箱）」
- 斗法台：「打不过就先去推图…不会变成死墙」→ 删；「守擂者按你战力换算…」→ 删
- 求签：「签文分五档…只算当天」→ 只留「签文分五档（大吉 → 末吉）」等短标签
- 境界 / 渡劫：「境界不是人人相同的公共阶梯…」→ 删；渡劫只剩「当前境界加成：+x%」
- 秘术 / 灯阁权限 / 转生：长段解释 → 一行短标签
- 设置与存档：「换设备前先导出…」→ 只留「进度只存在这台设备里」

`test_ui.js` 160 项全过。

## V9.5.5 · 2026-09-16 · 继续清「过度注释」（角色界面那几句操作说明）

父亲大人圈的四处，都是"教你怎么点"的小字，全删：

- 六维属性：「每升 1 级获得 3 点，每点 +2 维值；加错随时点右上角重置，不花任何东西。」整行删。
- 血族血统技能：「每升 1 级获得 1 点技能点；点错了点右上角重置退回全部技能点。」整行删。
- 等级条：「EXP 82% · 靠挂机与通关自动累积（当前 14.9 EXP / 分），每升 1 级自动 +3 属性点与 1 技能点。」
  → 只留数：`EXP 82% · 当前 14.9 EXP / 分`。
- 装备卡：标题右侧的「点方块换装」删掉（方块本身点得动，不用教）。

`test_ui.js` 那条"重置按钮旁边要写清能退回"改成断结构：两张卡里各有一个「↺ 重置」按钮。

## V9.5.4 · 2026-09-16 · 世界图标居中 + 清掉"操作说明"类小字

父亲大人：「这种就叫过度注释」——界面上教人怎么点的小字，一律去掉。

- **深井/残域卡片里图标与文字没对齐**：图标盒里是 emoji，行盒偏高显得图标往下掉；
  给 `.world-ico` 补 `line-height: 1`，图标真正居中。
- **删掉的操作说明**：背包底部「末尾的「＋」是扩容」、装备栏底部那段用法、
  关卡页「进关之后一口气打到最后一波…」、伙伴页「重复招募…」「六维由稀有度…」、
  装备锁定说明、「一件装备只能一个人穿」、「进度已经保存…」、
  成长页「点任意一条看细节」、队伍页长按说明与「点名字看详情」。
- `test_ui.js` 跟着改：两条断言原先拿被删的文案当锚点，现在改成断言结构
  （装备区不许有 list-row；长按换位断言 `armLongPress` 已接线）。

## V9.5.3 · 2026-09-16 · 整块主角卡都能点进角色界面

父亲大人：主页最上面那块卡片应该整块都能点进角色界面，现在只有【主角】那行的黄字能点。

- `heroBlock()` 里的 `data-protag="1"` 从【主角】那一行**移到卡片本身**：
  【境界】【等级】【主角】【转生】四行随便点哪里都进主角详情（加点 / 洗点 / 血统 / 境界）。
- 四行不再挂 `static`（那是"看着不可点"的样式），整块卡片 `cursor:pointer`。

## V9.5.2 · 2026-09-16 · 背包/执灯者主标签改矩形卡片并吸顶 · 修卡片贴一起 · 去掉啰嗦说明

父亲大人看界面时提的四件事，都在这一版：

- **背包（道具/材料/装备）与执灯者（队伍/伙伴/成长）顶部三个主标签**：
  从圆角胶囊改成**矩形卡片**，并 `position: sticky` **钉在顶栏（货币条）下方**——
  翻到下面挑东西时这一排不动，随时能换栏、也随时知道自己在哪一栏。
- **首页「主角卡」和「主线任务卡」贴在一起**：`.text-rows { margin: 0 }` 把 `.card` 自己的
  14px 下边距一起清掉了。改成保留卡片下边距（`.card.text-rows` 现在与其它卡片间距一致）。
- **去掉啰嗦的界面说明**：挂机卡那句「离线也算：…」、养成段尾整段「血统与境界属于主角自身…」、
  未解锁后面的括号解释、队伍页的长按说明与预设说明、背包底部的用法说明——
  一律删掉或压成一行（保留一句「全部养成线的总览在「执灯者 → 成长」」作为指引）。

`scripts/test_ui.js` 的断言跟着改：主标签现在断言 `tab-cards/tab-card`（吸顶矩形卡片）。

## V9.5 · 2026-09-15 · 修：一次全量体检（2 条会白送数值的、5 条会吞玩家东西的）

父亲大人要求"审一遍还有什么 bug"。只读审查确认了 13 条真问题，这一版全部修掉，
每条都配了能**先失败、后通过**的回归用例（不是"补一条断言让它变绿"）。

**P0 · 会破坏养成公平**

- **新档第一次渡劫后重开游戏，境界被 ×4**（`core.js` 的 `realmScaled`）。
  旧档换算（10 大境 → 36 小阶）靠一个"读档时才写"的标记防重复，而 `newGame()` 不跑迁移，
  于是新档第一次读档时标记还是空的，把当前境界也乘了 4：实测渡劫 5 次 → 重开变 **20 阶**（白送 +21% 全属性）。
  修法：建档时就把 `realmScaled` 落上（标记**不能**放进 `defaultState`，否则老档也带着它、永远不换算）；
  回归用例里同时钉住"真老档仍然按 3 境 → 12 阶换算"。
- **离线 1~5 分钟的收益被静默丢掉**（`main.js` + `core.js` + `ui.js`）。
  入账写在 `UI.showOfflineGains`（弹结算窗的地方），而 `settleOffline` 已经把时间戳推到当前时间；
  不弹窗 = 收益算完就扔。实测离线 4 分钟：56 点 + 64 经验全没了。
  修法：**入账搬进 `settleOffline`**，弹不弹窗只决定"要不要打扰你"。

**P1 · 功能与说明对不上**

- **扫荡显示 EXP、实际一点经验都不发**：`exp` 只写进给界面看的 `got`，扫荡路径没有加经验的那两行。
  实测扫荡 ×3 显示 `EXP+318`、实际 +0。现在按手打普通波的口径真发（角色 +318 / 主角 +159），
  并且**扫荡也会累计战斗次数**（否则扫荡党永远完不成"战斗 5 次"这类日常/成就）。
- **W15~W20 的世界机制完全没实装**：`Battle.MECHANICS` 只写到 W14，而世界表有 20 个世界——
  血月旧堡的吸血、沉海废墟的水压、蜂群主控的无人机群、白墙疗养院的幻觉、星骸坟场的护盾、灯阁王座的规则改写
  **一个都没生效**，最后 180 关实际是纯数值怪（界面上却照常写着机制）。
  这一版把 6 条全部补进引擎（含"幻觉：概率攻击同伴""水压：每回合全队掉血""轨道扫射：按节奏群体技能"）。
- **主角吃不到阵型加成**：`buildAllies` 的招募角色分支有 `fb.atkPct/hpPct/skillPct`，主角分支没有，
  而队伍页照常写"攻击+6% 生命+6%"——主角还正是让五行归元阵成立的万能补位。
- **背包满时奖励道具静默蒸发**：`applyRewardObj` 不判 `addItem` 的返回值，
  每日/周常/登录/成就/图鉴/悬赏/游历的物品奖励全走这一个入口；药园收获是同款（不看返回值、地块照样清空）。
  **新增「📮 待领箱」**：装不下的东西一律先存这里（含挂机产线材料），背包页点「全部领回」拿回；
  核心层多了一条 `notice` 广播，背包满、存档写不进去都会当场告诉玩家。

**P2 · 体验与一致性**

- **W14 随机规则第三条**写着"敌方速度提升"、实际加的是攻击；而且 `spdUp` 这一档以前**根本没人读**
  （`getBuffs` 里没有 `spdPct`）。现在补上 `spdPct` 结算，顺带修好
  「剑心通明（速度+20%）」「念动屏障（速度+25%）」这两个一直在空转的技能。
- **治疗/辅助攒不出必杀**：能量只在 `dealDamage` 里发，放治疗/增益/护盾等于白放。
  实测 30 回合治疗者 5 次、战士 9 次；修完两边都是 9 次。
- **最后一波喝药 = 白喝**：战斗是一次算完的，药剂只能作用于下一波，而最后一波打完 `run` 就清空。
  收官战不再给按药剂，并写明"药剂要到下一关才生效"。
- **药剂会复活阵亡成员**（`0 + 0.2 = 活了`），星级里的"无人阵亡"形同虚设。现在只治活人。
- **打完一关直接关掉 App，"继续上次副本"卡片还挂着**：结算完成即清掉落盘进度。
- **抓起后立刻点目标格会被吞掉**（700ms 的兜底计时器把新手势的点击也吃了）：新的一次按下即清标记。
- **装备页筛选后仍补满空格子**，看着像筛选没生效：筛选态下不再补空格。
- **结算倒计时 5 秒 → 8 秒**（想看清掉落的人来不及）。
- **新档第一天的登录奖励要等到第二次开游戏才弹**：新档老档统一走"等弹窗收干净再发"。
- **存档写不进去时没有任何提示**（隐私模式 / 配额满）：现在提示一次并引导导出存档。
- **GM 面板跟着正式包上线**（线上连点版本号 7 次就能开，还能刷货币并导出存档）：
  线上改成必须带 `?gm=1` 才认，本地开发照旧直接开。

测试：`test_game` 484 项（新增 29 项）、`test_ui` 160 项（新增 7 项），全绿。

## V9.4 · 2026-09-15 · 修：新一波的血条显示成满血（血量"被刷新"）

父亲大人报的问题：副本打到一半进新一波，血条又变满了。

**根因（两层）**

第一层在引擎里：`Battle` 造单位时把 **`maxHp` 直接写成了当前血量**（`maxHp: spec.hp`）。
敌人永远是满血生成，所以一直没暴露；但副本是**带血打下一波**的——
一个 400/1000 的成员进场，引擎记成 `400/400`，这一波打完把 `hp/maxHp` 写回去就是 **100%**。
于是**每一波都会把上一波挨的伤抹掉一部分**，血线自己往上涨——这才是"血量被刷新"的真相。
（顺带还有个隐形损失：战斗内的治疗、吸血都被压在"进场血量"上，回不了真上限。）

第二层在画面上：每格血条的初始宽度**写死 100%**，只有挨打时才会被更新，
所以带血进场也看不出来。

**修法**

- `maxHp: spec.maxHp || spec.hp`：有真上限就用真上限（敌人不传，照旧满血生成）
- 血条按**进场时的真实血线**画，低血（<35%）直接进红色"低血"样式；
  血条下面补一个**百分比数字**（血量变化时同步更新）
- 带血进场时在战斗日志里写一行「🩸 带血进场：某某 30% · 某某 78%」，
  明确告诉玩家血是继承来的；满血进场不写，免得多一行废话

测试：`test_game` 455 项（新增 3 项：maxHp 不被压成当前血量 / 40% 进场仍是 400·1000）、
`test_ui` 153 项（新增 1 项：带血进场时血条画真实血线），全绿。

## V9.3 · 2026-09-15 · 装备页重排（分类上移、列表撤掉、格子勾选）

父亲大人这一轮只提了一件事：背包装备那一页的层次不对——分类和批量分解压在内容下面，
底下还留着一串"一行一件"的列表，跟上面的格子重复。改法：

### 三层结构

- **① 三个主标签**（道具 / 材料 / 装备）：横向铺满、平均分布（原来按文字长短自适应，右边空一大块）
- **② 分类胶囊**：紧贴在主标签正下方，**按钮小一号**——"分类属于上层、内容属于下层"一眼分得出来
  - 第一排是类别（全部 / 普通 / 世界套装 / 职业套装 / 专属），第二排是部位（武器 / 胸甲 / 头部 / 手部 / 腿部 / 饰品 / SSR+）
  - **「🧹 批量分解」也搬到这一层**，跟分类挨着，不用再往下找
- **③ 内容只有格子**：原来那串"一行一件"的列表**撤掉了**；点格子进详情

### 穿在身上的装备不算背包物品

- 穿在角色身上的装备**不在背包里出现**，**也不占背包格数**（右上角写的是"未穿戴 N / 上限格"）
- 要回到背包，先在角色卡里卸下；装备格满了以后，新掉落的装备会照旧自动分解成 ◆异界结晶

### 批量分解改在格子上勾

- 列表撤了以后，**批量态下格子本身就是勾选框**：点一下选中（红框）、再点取消，锁定的格子灰掉不可选
- 底部仍然有"快选 N / R / SR、清空、分解、取消"那条操作栏

"执灯者"页的三个标签也一并改成横向铺满，跟背包保持同一套视觉语言。

测试：`test_game` 452 项、`test_ui` 152 项（新增 3 项：装备页层次、穿着装备不占格、格子勾选），全绿。

## V9.2 · 2026-09-15 · 背包分三池格子制 + 副本无缝连波 + 一批逻辑漏洞修复

父亲大人这一轮提了两件事（背包不行、战斗站位看不出来、连波别一波一波结算），
外加"上次报的问题一起改完"。

### 一、背包重做：三池格子制

- **道具 / 材料 / 装备分三池**，各自 **50 格**起、**各自扩容**（不再共用一个池子互相挤）
- **格子制**：一整片方格，有东西的格子写「名字 + 数量」，空格子留白；
  **末尾多一格「＋」就是扩容按钮**，一次 **+10 格**——扩到 60 格时，第 61 格就是新的「＋」
- 格子只写名字和数量，**介绍、在哪用、批量使用（1 / 10 / 全部）都在点进去的详情页里**
- 装备池只放"没穿在身上的"（穿着的在下面那张总览表里看，不占背包格）
- **背包里的「货币」那一块撤掉**（顶栏已经有「▤ 全部货币」）；**炼化台搬到主页「养成」段**

### 二、战斗：站位看得见 + 无缝连波

- **站位可见化**：战斗界面从"一整条平铺"改成**三行**——上排敌人、中间「我方后排」、
  下排「我方前排 · 敌人优先打这里」。队伍页排的站位，在战斗里一眼看得出来
- **无缝连波**：波与波之间**不再弹结算页**，这一波打完只飘一行「第 N 波已通过」，
  0.9 秒后直接接下一波；只有整关打完才出结算（胜利 / 失败 + 星级 + 再来一次 / 下一关）
- 满屏刷本时最烦的"点一次结算、点一次继续"没了；波次收获写进战斗日志
- **补血手段跟着搬**：战斗界面底部新增**「战备补给」**条，副本全程可用。
  因为一场战斗的帧是开打前一次算完的，喝下去的药**从下一波进场时生效**（条上写明了这点）

### 三、修掉上一轮体检报出的漏洞

- **P0 刷关**：通关奖励（每个世界 · 每个难度）改成**只发一次**。之前重复刷已通关世界的第 12 关
  会一次次重发（普通档 ✦100 + ❖500 + ◆200 + ▣100）——等于无限刷高级货币。老档在迁移时
  把"已经打穿的世界"直接标成已领，不会因此白给一轮
- **P0 角色消失**：抽到「林默」（C001）后下次读档会被删掉——旧版"主角占位"的清理代码**每次读档都跑**。
  现在加了只跑一次的开关
- **P0 解锁门禁**：`装备强化`（通关菌毯巢穴·第 3 关）与`血统强化`（通关潜影窟·第 1 关）
  两把锁过去只写在表里、没人执行，现在真的会拦（选血统本身不受限，那是开局必经一步）
- **P1 每日重置**：`dailyDate()` 从 UTC 改成**本地日期**。之前每日任务 / 商店 / 求签 / 斗法台 /
  扫荡 / 登录要等到北京时间早上 8 点才翻新，免费招募却在零点翻新——两套钟，界面还写着"每天 0 点重置"
- **P1 主线解锁提示**：弹窗读的是"下一条任务"的解锁字段，错位一格；现在由 `claimQuest`
  真的发放并返回"这次解锁了什么"，弹窗照着弹
- **P1 存档导出**：设置页的「📤 导出 / 📥 导入」和三个**手动存档槽**补上了（菜单里早就写着"导出"，
  后端函数也都有，就是没接口）；导入 / 读槽现在也会走一遍存档迁移
- **P1 免费招募出率**：改成与普通池同源（之前是 N50/R42.5/SR7.5，界面却写"走普通池出率"）
- **P2 十连保底**：保底 SR 是把最后一次换掉，不再白送第 11 个人（旧写法会多入库一个）
- **P2 星级**：中间波倒过人也会算进本关战绩，不再只看最后一波
- **P2 深井 W05**：飘字「☠ 即死」与实际效果（打到 1 血）不符，改成「⚠ 濒死」
- **P2 灯录**：总数只算抽得到的角色（原来把 6 个永远拿不到的隐藏角色也算进去，永远集不满）
- **P2 换名残留**：轮回小队 / 轮回之袍 / 轮回头盔 / 轮回权杖 / 轮回盘 / 轮回之种 / 生化系列 /
  基因系列等旧名字换成新世界观用词（终焉 / 血脉 / 聚合物…）
- **P2 死数据**：20 多条旧奇遇事件（里面还留着"安全屋"这种早就没有的场景）、对应的弹窗函数、
  没人用的副本宝箱节点与两个波间概率常量，全部清掉
- **P2 玩法指南**：第 ① 章里同时写着"点底部按钮打下一波"（旧）和"一口气打到底"（新），
  自相矛盾；章节号缺 ⑧ ⑫ 也补齐了

### 四、命名统一

- 招募来的角色 → **伙伴**（主角本人就是执灯者，两边不再混用同一个称呼）
- 「执灯者」页的子页：队伍 / **伙伴** / 成长（装备挪去了背包）
- 招募入口 → **「招募伙伴」**；主角列表 / 新建主角（原"角色列表 / 新建角色"）

> 测试：`test_game.js` 452 项、`test_ui.js` 149 项，全绿。
> 另外用 390px 宽的手机视口实际截图核对了背包格子、战斗三行站位与连波轨迹。

## V9.1 · 2026-09-15 · 玩家身份改叫「执灯者」

父亲大人看完 V9.0 的界面说「记名者听着好 low，换一个」——确实，气质跟"灯阁"接不上。

- **记名者 → 执灯者**：灯阁把你送进残域，你手里提着一盏灯。底部页签、招募页、
  开场白「欢迎回来，执灯者」、角色卡上的「执灯者本人」、血清/经验道具的说明全部跟着改（共 52 处）
- **记名录 → 灯录**（同一个道理，"记名"两个字太素）
- **轮回招募券 → 引灯招募券**（券名里最后一个"轮回"也换掉了）
- 主角默认名字也跟着改成「执灯者」（可以在角色卡里随时改名）

> 测试：`test_game.js` 443 项、`test_ui.js` 148 项，全绿。

## V9.0 · 2026-09-15 · 副本一口气打到底 + 全站换名（自创世界观「灯阁 / 残域」）

父亲大人两条要求一起做完了。

### 一、副本改回"纯粹"：打完自动进下一波

- **不再有「开打 · 第 N 波 / 开打 · 最后一波」这种要按的按钮**：点进一关就一口气打到最后一波，
  中间自动接上（波间结算页有 3 秒倒计时，也可以直接点「继续 · 第 N 波」）
- **波间不再插随机遭遇、不再插补给箱**：事件面板与补给箱面板都不再触发
  （事件数据与面板本身留着没删，以后想接到「游历奇遇」上可以直接用）
- **补血手段没有丢**：波间结算页上直接放**队伍血条 + 治疗剂 / 强化剂按钮**，倒计时里想补就补
- **顺手修掉一个老 Bug：波间血量继承其实一直没生效**。战斗快照（`publicUnit`）没带 `charId`，
  界面写不回 `run.hpPct`，所以每波都是满血开打——现在修好了，血量继承是真的了
- 因为"补给箱"这条产出没了，**治疗剂改挂到每一波战斗上**：普通战 20% / 精英 40% / Boss 必掉，
  深层给大的。道具说明里的「去哪弄」也一并改成实装来源（不再写"随机事件 / 副本宝箱"）
- 玩法指南与主线引导里"途中会遇到安全屋 / 随机事件"的旧文案一并改掉

### 二、换掉所有会让人联想到《无限恐怖》的名字，自创一套世界观

世界观：**被「灰潮」抹过的地方会留下「残域」**；玩家被**灯阁**选中，成为**记名者**，
进残域把还没被抹掉的东西记下来、带回来变强。

| 旧 | 新 |
|---|---|
| 无限轮回（游戏名） | **残域** |
| 主神空间 | **灯阁** |
| 主神评级 / 主神权限 / 主神商店 / 主神恩赐 / 主神核心 | 灯阁评级 / 灯阁权限 / **灯阁市集** / 灯阁恩赐 / **灯芯** |
| 轮回者 | **记名者** |
| 轮回图鉴 | **记名录** |
| 轮回副本 / 恐怖世界 | **残域** |
| 无限回廊 / 回廊徽记 | **深井** / 深井徽记 |
| 基因锁 | **铭刻** |

- **20 张地图全部改名**（原来 生化蜂巢 / 异形巢穴 / 咒怨凶宅 / 死神游轮 / 猛鬼梦境 / 寂静雾镇 /
  侏罗纪孤岛 / 狂蟒沼泽 / 加勒比亡灵船 / 魔戒远征 / 纳尼亚王座 … 全部踩别人 IP）：
  现在是 **菌毯巢穴 / 潜影窟 / 怨声旧宅 / 机关地宫 / 无归客轮 / 轨道残骸带 / 酣眠迷境 / 哑雾小镇 /
  巨兽孤屿 / 毒沼深处 / 骷帆船坞 / 蚀环远征 / 寒冠王座 / 灯阁试炼场 / 血月旧堡 / 沉海废墟 /
  蜂群主控 / 白墙疗养院 / 星骸坟场 / 灯阁王座**
- **敌人与 Boss 一起改**：舔食者 / 抱脸虫 / 三角头 / 无脸护士 / 戒灵 / 黑珍珠大副 / 深潜者 等
  全部换成自创名（裂舌兽 / 伏面虫 / 钩索巨影 / 白面护工 / 蚀环幽灵 / 骷帆大副 / 深渊潜者 …）
- 开场白、玩法指南、README、内部文档全部对齐；**存档 key 没动，老存档照常读**

> 测试：`test_game.js` 443 项、`test_ui.js` 148 项，全绿。

## V8.9 · 2026-09-15 · 装备改方块 + 角色卡重排 + 主页去重复入口

父亲大人这一轮的四条：

**① 装备改成方块（两行三格）**

- 原来主角与伙伴的装备都是「一行一件、点一行换装」，现在改成 **6 个方块铺成两行三格**
  （上排：武器 / 头部 / 胸甲，下排：手部 / 腿部 / 饰品）
- 主角与伙伴**共用同一套组件**，改一处两边都变；方块上写清部位、装备名（按品阶染色）、主属性
- 点方块＝换装；右上角「卸下」＝脱掉。**卸下按钮本体只有 30×30，热区靠 `::after` 撑到 44×44**
  （手机上字小但点得准，是全站一贯的热区做法）
- 空槽显示「未装备」并走虚线描边，一眼看出"哪件穿了、哪件空着"

**② 角色卡顺序按需求重排**

- **基础信息与等级合并成一张卡**：头像 / 名字 / 战力 / 等级 / EXP 进度条 / 升级要点全在一张里，
  不再单开一张「📈 等级」
- 新顺序：**基础信息+等级 → 🎯 六维 → ⚡ 技能 → 🗡 装备 → 🩸 血统 → 🌌 境界 → 📊 属性面板**
  （伙伴没有境界，那一格是「⭐ 星级」，其余一致）
- 「📊 属性面板」挪到**最后**当总览，先看养成项、最后看汇总数值

**③ 洗点并进两张卡**

- 撤掉单独那张「↺ 洗点」卡；**六维卡与技能卡的标题右侧各放一个「↺ 重置」按钮**
- 两张卡的说明里各写一句"点右上角重置、点数一点不少"，按钮旁边就是它管的东西，不用来回找

**④ 主页去掉重复的血统 / 境界入口**

- 血统与境界**属于角色自身**，主页「养成」段不再放这两个入口（避免和角色卡重复）
- 主页顶部的【境界】那一行保留为**只读状态**（点不了，只是显示当前境界），
  未选血统时它的指引改成"点【主角】卡里选血统"；【主角】那一行也改成"点开：加点 / 洗点 / 血统 / 境界 ›"
- 主页养成段底部补了一句小字，说清血统 / 境界去哪设置

媒体自检：在 **390 / 340** 两种手机宽度下，主页、主角卡、伙伴卡三个页面**都是 0 处元素出格**
（临时套一层 iframe 才拿到真手机视口——macOS 上无头浏览器窗口有最小宽度，直接开 390 会被裁成假象）。

> 测试：`test_game.js` 443 项、`test_ui.js` 142 项，全绿。

## V8.8 · 2026-09-15 · 角色卡与主角卡：一屏总览 + 各系统独立卡片

父亲大人反馈：**点进角色卡内容太多太乱**，希望血统 / 境界 / 六维 / 技能 / 装备各占一张卡，
并且给一个**总的属性数值面板**。

- 招募伙伴与主角现在是**同一套骨架**，从上往下扫一遍就看完：
  头像与战力 → 📊 属性面板 → 🎯 六维 → 📈 等级 → ⭐ 星级（伙伴）/ 🌌 境界（主角）
  → ⚡ 技能 → ↺ 洗点（主角）→ 🩸 血统 → 🗡 装备
- **新增「属性面板」**：攻击 / 防御 / 生命 / 速度 / 暴击 / 暴击伤害 / 闪避 / 吸血 / 减伤 / 技能加成，
  十项两列排完（装备、血统、境界、基因锁的加成都已经算进去），一屏就能比数值
- 招募伙伴补了一张「**六维属性**」卡（他们的六维是固定成长、不用加点），并写明六维由稀有度 / 等级 / 星级决定
- 装备卡做成**共用组件**（伙伴与主角同一套，只是数据字段不同），标题上直接写「3/6 件 · 点一行换装」
- 面板里原来的 `section-title` 分节**全部取消**——一卡一段，不再是一长列小标题
- 回归测试锁住：两张卡各自至少 8 张独立卡、必须有属性面板、拆卡时不准把按钮弄丢

> 测试：`test_game.js` 443 项、`test_ui.js` 138 项，全绿。

## V8.7 · 2026-09-15 · 「游历」只留游历奇遇，其余日常收进「养成」

父亲大人问："游历里为什么会有悬赏、成就、求签、招募，这些不应该放在养成那边吗"——对，是康康上一版分错了。

- **「游历」段现在只有一条**：游历奇遇的进度条（它才是"游历"这个名字对应的事），下面一句小字说明
- **悬赏 / 每日任务 / 成就 / 求签 / 轮回者招募 / 兑换大厅** 全部收进「养成」段，
  在那一格里单起一行小标题「**日常**」隔开——上面是长期成长线，下面是每天要点的几件事
- 玩法指南里指向旧位置的说明（求签 / 限时悬赏）一并对齐

> 测试：`test_game.js` 443 项、`test_ui.js` 137 项，全绿。

## V8.6 · 2026-09-15 · 首页重排 + 去掉重复入口 + 主角洗点 + 全站手机适配

父亲大人提了三类问题：**主角六维和技能不能洗点**、**首页有重复入口和多余的电脑式提示**、
**界面在手机上还有贴在一起 / 文字出格 / 没适配好的地方**。这次一起做完。

### 一、首页按您定的顺序重排，一个功能只留一个入口

- 新顺序：**主角 → 主线 → 养成 → 游历 → 挂机 → 设置**
- 撤掉「今日」匾额：一键收取归到「挂机」那块，每日任务 / 悬赏 / 免费招募 / 求签收进「游历」那一段
- 去掉重复：以前"挂机分工"在挂机卡和下面菜单各有一个，现在只留挂机卡里的「派人分工」；
  顶栏的「设置 / 指南」两个图标按钮撤掉（首页最后一段是它们唯一的入口）
- 加了回归测试：**首页任何入口动作不许出现两次**，五段顺序也被锁住

### 二、长按换位不再弹提示框

- 长按抓起时不再弹 toast（手机上一弹框挡半个屏幕），抓起来的状态直接画在界面上：
  卡片高亮 + 顶部金色提示条 + 空位写「放这里」，手上再给一下轻震动
- 文案里"按 Esc 取消""用鼠标不方便"这类**只有电脑才有的说法**全部删掉（包括玩法指南里的那几处）

### 三、主角能洗点了

- **六维洗点**：把已经分出去的属性点全部退回，免费、可反复洗（`Core.resetAttrs`）
- **技能重置**：原有的重置保留，和新加的一起收进主角详情里的「洗点」一块，各自带一次确认

### 四、"电脑上看着对、手机上才出问题"的六类，全站扫了一遍

| 问题 | 改法 |
|---|---|
| `title=` 悬停才有信息（顶栏货币、境界小阶成功率） | 手机上没法悬停 → 信息直接写在界面上 |
| 只有键盘能做的操作（Esc） | 文案全删（键盘监听保留，给电脑用，不再宣传） |
| 热区靠伪元素硬撑（补 9px） | 会盖住上下相邻元素造成点错 → 改成"本体 40px + 补 2px" |
| 正文上边距写死 92px | 系统字号调大 / 刘海更高时会钻到顶栏底下 → 改成 JS 量顶栏高度写进 `--topbar-h` |
| 键盘弹出盖住弹窗（改名字时） | 用 `visualViewport` 算出键盘高度写进 `--kb`，居中弹窗整体上移 |
| 文字出格 / 卡片贴在一起 | 卡片间距放宽一档；文字行、按钮行、卡片全部加断行 / 省略号 / 换行兜底；新增 375 / 340 / 矮屏三档断点 |

### 五、顺带清掉的

- 删掉一批没人再用的样式（`.plaque*` / `.hero-num` / `.today-row` / `.status-strip` / `.tb-icon` 等），
  避免"样式还在、界面早不用了"这种误导
- 队伍页的预设按钮改成固定三列（不再被长标签挤成 2+1 两行）
- 玩法指南里指向旧入口的说明（"首页今日那块""顶部状态区的轮回"）一并对齐到新位置

> 测试：`test_game.js` 443 项、`test_ui.js` 136 项，全绿。

## V8.5 · 2026-09-15 · 图标改矢量 + 可装到手机（PWA）

父亲大人反馈"**图标还是歪的**"，并问"**能不能最终输出成手机能装的包**"。

### 一、图标改成矢量 SVG（这次是根治）

上一版改成"用两根 CSS 线拼一个 ×"，理论上是中的，但手机屏幕的像素比常常是 2.6x、3x 这种
**非整数**：两根线各自会被推到半个像素上，于是看上去还是"叉歪了"。

- 现在关闭键和返回键都是**内联 SVG**：两个图形在 24×24 画布里都以 (12,12) 为中心，
  线条坐标对称、不经过任何字体、也不依赖像素比 → 任何机型任何缩放下都在正中
- 顺带加了回归测试：坐标必须对称、必须是 SVG（不许再退回字符或拼线）

### 二、手机上"改了却没变化"这件事，一起修掉

- **资源带上版本号**（`js/ui.js?v=8.5` 这样）：手机浏览器对 CSS/JS 缓存非常凶，
  不带版本号时更新推上去了、手机上还是旧的，看起来就像"改了没用"
- 设置页最底下的版本号就是判断依据：**显示 V8.5 才是最新**（测试会保证"资源版本号 = SW 版本号 = 页面显示版本号"三处一致）

### 三、可以装到手机上了（PWA，等价于"手机 App"，不需要签名）

- 新增 `manifest.webmanifest` + 主屏图标（180 / 192 / 512，康康画的"轮回环"图标）
- 新增 `sw.js` 离线缓存：**第一次打开之后，断网也能玩**
- 手机上这样装：
  - **iPhone**：Safari 打开游戏 → 分享 → **添加到主屏幕** → 桌面上出现图标，点开就是全屏、没有地址栏
  - **安卓**：Chrome 打开 → 右上角菜单 → **安装应用 / 添加到主屏幕**
- 关于 **IPA**：真正的 `.ipa` 需要 Xcode + Apple 签名才能出包，本机目前**没有装 Xcode**。
  上面这条 PWA 路线得到的体验（桌面图标、全屏、离线）与自签 IPA 基本一致，且不用证书、不会 7 天过期。
  若您要的就是 `.ipa` 文件本身，康康可以把工程按 Capacitor 的目录准备好，等装上 Xcode 就能一条命令出包。

> 测试：`test_game.js` 439 项、`test_ui.js` 128 项，全绿。

## V8.4 · 2026-09-15 · 手机端修正：关闭 / 返回图标不再偏，热区与输入框一起补齐

父亲大人反馈：**"手机上关闭按钮里的 X 是偏的，电脑上不会"**——游戏的用户主要在手机，
所以这次专门把移动端最容易出问题的地方过了一遍。

### 一、图标为什么只在手机上偏

- 原来的关闭键和返回键是**用字符画的**（`✕` 和 `‹`）。字符在不同系统会落到不同字体，
  行高与字面框都不一样，再加上 iOS 给 `<button>` 套的默认外观（内阴影 + 自己的行高），
  结果就是"电脑上正、手机上偏"
- 现在两个图标**改成用 CSS 画**：`×` 是两根 45° 短条，交点用 `translate(-50%,-50%)` 钉在按钮正中；
  `‹` 是正方形只留左、下两条边再转 45°。**跟字体彻底无关，任何机型都在正中**
- 顺手清掉了系统默认外观（`appearance: none`），按钮不再被系统改圆角与内阴影

### 二、手机热区补齐（原来有几个点太小）

按本项目"热区 ≥44px"的规则，把不够的都用伪元素把点击区撑大（视觉尺寸不变）：

- 关闭键 34px → 点击区 44px；返回键 40px → 44px
- 顶栏「指南 / 设置」高 34px → 点击区 44px；货币小胶囊高 26px → 点击区 44px
- 页签胶囊（队伍 / 角色 / 装备 / 成长）40px → 44px

### 三、手机上另外两个老毛病

- **点搜索框整页会"跳"一下**：iOS 遇到字号 <16px 的输入框，聚焦时会把整页放大。
  现在输入框统一按 16px 渲染，绕开这个系统行为（桌面上几乎看不出差别）
- **点了有 300ms 迟滞**：按钮与输入框统一加 `touch-action: manipulation`，点起来跟手；
  长按也不再选中文字 / 弹系统菜单

> 测试：`test_game.js` 439 项、`test_ui.js` 121 项，全绿。

## V8.3 · 2026-09-15 · 站位改成「长按拖拽」· 主角可以站后排 · 一件装备只能一个人穿

父亲大人玩过之后提了三件事，外加一句补充：**"上阵这里长按拖着移动到对应位置松手就放下，
不是长按完还要再去点一下位置"**。

### 一、换位改成「长按抓起 → 按住拖到位置 → 松手放下」

- **长按 420ms 抓起**：手指按住任意站位（含主角）不松，那一格会抬起来，上方出现
  「已抓起「某人」· 拖到别的位置松手放下」的提示条
- **拖着走**：拖动过程中，指针底下的那一格会实心高亮（其余格子变虚线，表示"都可以放"），
  空位置直接把字换成「放这里」——**松手就落在高亮的那一格上**，不用再点第二下
- **拖回原地**＝放回原位；**Esc / 提示条的「取消」/ 点自己那一格**也能放回去
- 备用路径保留：抓起之后**点**一下目标站位也能放下（鼠标不方便拖、或者手滑松早了的时候用）
- 移动端手势不打架：长按之前滑动＝正常滚页面（自动取消长按），长按之后拖动才接管

### 二、主角可以站后排了

- 以前主角被钉在前排，而界面又写着"后排相对安全"——说了安全却换不过去，这次改掉：
  **主角跟前排/后排都由玩家定**，战斗里真的按选择的排站位（前排吃伤害、后排躲伤害）
- **上阵固定 5 格：前 2 后 3**。主角必上阵，他自己就占其中一格——所以主角站前排时前排是
  「主角 + 1 名队友」、站后排时后排是「主角 + 2 名队友」，**任何时候都不会多挤出一格**
- 主角那张牌跟队友的格子一样能拖：拖到后排第 3~5 格就站后排，拖回第 1~2 格就回前排；
  跟队友换是"两格互换"，拖到空位就是搬过去、原位留空。规则在队伍页与玩法指南里都写清了

### 三、一件装备只能一个人穿（BUG）

- 症状：从角色 A 身上把装备拿给角色 B，A 身上那件没有被摘掉，导致**同一件装备被两个人同时穿**
- 修法分两处：**写入端**统一走一个"从所有人身上摘下"的动作（装备给谁之前先把这件从别人身上摘掉），
  **读档端**加一次去重兜底（老存档里的重复穿戴自动修好，进游戏就正常）
- 顺带修了「一键最优装备」的老毛病：它以前会造重复分配，现在先把未锁定的装备全部收回再统一分配，
  锁定中的装备一件都不动

### 四、队伍页顺序与引导文案

- 「成员一览」移到「阵型」**前面**（先看人，再看阵法）
- 上阵 5 格的显示：前排一行 2 格、后排一行 3 格，两行标题上直接写着「2 格 / 3 格」；
  主角那一格描金并标注「主角 · 前排/后排」
- 老存档自动迁移：原来 4 格（主角不占位）的队按他当时站的排插进 5 格里，队友顺序不变；
  编队预设也跟着升级成 5 格
- 玩法指南第 ② 章、主线引导、队伍页提示语全部改成"长按拖拽"的说法，说明与实装同源

### 五、整体体检（数值 + 界面，父亲大人要求"从数值到界面都审一遍"）

**数值**

- 🔧 **药园产量是笔亏本买卖（已修）**：旧产量只有投入点数的 **1 成~5 成**——花 ◈800 种一块地，
  收上来的材料按"强化替代价"只值 90~720，等于把点数往里扔。现在改成**每块地的收货价值约为投入的 1.2 倍**
  （下品 5×基础金属 / 中品 8×强化合金 / 上品 12×异界合金 / 极品 16×虚空晶体），稀有的兽魂石、
  装备箱几率照旧；并加了回归测试，以后产量再被改低会当场报错
- 📏 量级复核（都用脚本算过，不是估的）：满配挂机 **9,041 点/时 + 11,150 经验/时**；
  练满一名角色需要点数 21.3 万 + 经验 148.8 万，折算约 **130 小时**挂机（点数侧 23.5h，经验侧 133h）；
  境界全链 36 阶共 **389 万点 + 502 材料**（与旧 10 境的 387.8 万点对齐）；秘术阁全满 **13.6 万异界结晶**；
  主神权限 1→10 共 **3,797 圣洁晶石 + 2,530 异界结晶**；基因锁五阶 100→5000 血统结晶；天赋单树 6,200 转生点
- 📏 战斗曲线复核：新手 Lv.1 单主角能打过 W01 第 1 关；满配 Lv.100 裸装打不过 W20 守关 Boss、
  配上 6 件 UR+20 能过——"装备是最后一道门槛"这个设计是成立的

**界面**

- 🔧 主线"上阵"引导的高亮锚点原来是第 1 格，可主角默认就站在第 1 格，锚点可能指不到（高亮不出现）；
  现在改指「前排」这一整排
- 🔧 玩法指南里"血统升级"那条还写着旧数值（每突破一境 +5%），改成与实装一致的"每小阶 +1.4%、36 阶 +50.4%"
- 🔧 药园面板原来只写"花多少"，没写"收什么"——现在每块地都写明 `◈价格 · 时长 → 收 xx×N · 概率出 xx`
  （新增测试：说明必须与数据表同源，改产量忘了改文案会当场报错）
- ✅ 复查了首页 / 副本 / 轮回者（队伍·角色·装备·成长）/ 背包 / 药园 / 斗法台 / 法宝 / 坐骑 / 主神权限
  九个界面：文字层级、前后排 2+3 显示、消耗与产出标注都一致，没有发现 undefined 或错位

> 测试：`test_game.js` 439 项、`test_ui.js` 118 项，全绿。

## V8.2 · 2026-09-14 · 补齐对标产品的剩下几条线 · 结算 5 秒自动接关

父亲大人说"缺的和数量少的都补上"，另加一条：**副本胜利结算倒数 5 秒没选就自动进下一关**。

### 一、胜利结算：5 秒自动接下一关

- 结算页的**主按钮**（默认就是「› 下一关」）带一个倒计时徽章，从 5 秒开始跳，跳到 0 自动开打下一关
- 手动点任意按钮（再来一次 / 下一关 / 收下奖励并返回）就取消倒计时，不会抢你的选择
- **只有胜利才自动跳**；失败页不动。最后一关打完没有"下一关"时也不跳，不会把人越推越远
- 设置里新增开关「通关结算自动进下一关」，默认开启，随时可关（关掉后结算页会一直等你点）

### 二、补上对标产品里有、我们没有的五条线

对照《道友修仙》的数据表逐条对了一遍，缺的是这五条，这次一次补齐：

- **🌱 药园**（对标"洞府·药园"）：4 块灵田，花 ◈点数种下，到点收强化材料（离线也计时），另有几率出兽魂石 /
  装备箱；「一键全收」一次收完。它给点数开了第三个出口，也让强化材料不用死刷副本
- **🥋 斗法台**（对标"斗法 / Arena"）：单机做不了真 PVP，所以做成**镜像擂台**——守擂者按你自己的队伍战力换算，
  台数越高越强。每天 5 次，赢一场升一台拿 ◆异界结晶 + ♜回廊徽记，输了退一台（**保底第 1 台，永远不会卡死**）
- **🔮 法宝**（对标"FaBao"）：装备给数值、法宝给**效果**（吸血 / 开场能量 / 减伤 / 闪避）。主角带 1 件，
  花 ◆异界结晶买，买了自动戴上，随时能换能摘。20 件，R→UR 四档
- **🐎 坐骑**（对标"Horse"）：法宝给效果、坐骑给**基础数值**。和法宝不同的是**坐骑全队通用，招募角色也吃**，
  所以资源紧时先买坐骑更划算。花 ◈点数 + 强化材料驯服，高阶额外花 ◆异界结晶；7 匹，只骑 1 匹随时换
- **🎋 求签**（对标"SignItem"）：每天免费摇一次，五档签文（大吉 4% → 末吉 34%）给**当天的挂机加成**
  （+6%~+30%）与一笔硬通货。面板上直接公示每档概率，不用猜；隔天自动失效

新补的还有几处"数量太少"：

- **世界 14 → 20 个**：新增 血月古堡 / 深海遗迹 / 天网中枢 / 灵异病院 / 群星坟场 / 主神王座，
  难度曲线接着往下走（共 20 × 3 难度 × 12 关 = 720 关），世界套装也跟着长到 20 套
- **道具 34 → 39 种**：新增 全效治疗剂 / 合金护盾剂 / 狂暴催化剂 / 超频注射剂 / 究极经验模块，
  每一件都有真实来源（副本掉落 / 补给箱 / 商店）与真实用途（探索增益 / 喂角色），不留死道具
- **每日任务 6 → 8 条**：新增「求签 1 次」与「斗法台守擂 1 次」，让新补的两条线也进日常循环
- **秘术阁 42 条 / 游历奇遇 40 种 / 法宝 20 件 / 角色 120 名**（这些在 V8.0~V8.2 期间已经补厚）

### 三、顺带修掉的问题

- 首页「成长」里境界那一行还在用旧写法读名字，非修真血统会显示错名字（现在统一读当前血统的境界名）
- 副本列表标题写死"恐怖世界（14）"，加了世界也还是 14（现在跟着实际世界数走）
- 设置面板的 `settingsModal` 忘了 `return`，测试拿不到面板对象；顺手补上
- 设置里切换"大额消费二次确认"时，提示文案一律说成"自动战斗"，现在按开关名报
- 坐骑面板第一版把带图标的消耗文案塞进了 `title` 属性，属性被 HTML 引号截断，界面出现 `◈8000">` 这种乱码
  （**教训：带 HTML 的文案只能放正文，不能放进任何属性**），已改成"消耗写正文、按钮只写动作"

### 四、测试

- `test_game.js` **396 项**（本版 +70）：药园（播种 / 没熟不能收 / 熟了能收 / 点数不足 / 一键全收）、
  斗法台（每日次数 / 赢升台拿奖励 / 输退台保底 / 次数用完）、法宝（买要结晶 / 自动佩戴 / 换佩戴效果立刻变 / 不能重复买）、
  坐骑（货币 + 材料两层门槛 / 全队生效 / 不能重复驯服）、求签（每天 1 次 / 真进账 / 当天挂机加成 / 跨天失效）、
  世界与道具数量与完整性校验
- `test_ui.js` **102 项**（+5）：结算倒计时（5 秒常量 / 只认胜利 / 没下一关不跳 / 按钮带秒数 / 设置里能关）、
  坐骑与求签面板渲染、首页与成长页入口、指南收录新章节

## V8.1 · 2026-09-14 · 副本改成「点进去就打」· 境界跟着血统走 · 界面改成纯文字

父亲大人指出三件事：① 副本的打法和逻辑没变；② 境界不该是"凡人"，应该先选血统、再走对应血统的境界线；
③ 界面太乱、挂机块占太大，找不到功能——"跟他一样，不要图标，就纯文字，简单明了"。

### 一、副本：从"三步选路线"改成"点进去就打"

对标产品的副本没有"先选路线"这一层，点进去就是战斗。我们照做：

- **点关卡 = 直接开打第一波**，不再让你在遭遇战 / 精英 / 事件 / 宝箱 / 安全屋之间做三次选择
- 每关 **1~3 波连续战斗**：1~4 关 1 波、5~8 关 2 波、9~12 关 3 波；第 4/8 关最后一波是精英、第 12 关是守关 Boss
- **血量在波与波之间继承**——这是"连打"的重量所在，也让治疗剂 / 强化剂仍然有用
- 中间波只给普通战斗掉落，**最后一波才结算星级与整关掉落**（和扫荡口径一致）
- 途中小概率插一次随机遭遇或补给箱：出现就出现，**不拦路**
- 删掉 `genRoute` / `battlePreview` / 路线节点与"敌情预告"这套只服务于选路的东西

### 二、境界跟着血统走，不再有"凡体"

- **开局第一件事就是选血统**（原来是 Lv.10），因为境界线由血统决定，不能拖到中期
- 每条血统自带 9 大境：血族 血奴→血仆→血卫→血将→血侯→血王→血皇→血帝→血祖；狼人 兽崽→…→狼神；
  修真 炼气→…→渡劫；魔法 学徒→…→秘神；科技 改造体→…→神机；念动力 感应者→…→念神
- 每大境 4 小阶（初期 / 中期 / 后期 / 大圆满），**第 1 阶就是这条线的第 1 境·初期**，共 36 阶
- 没选血统时状态行显示「未定血统」，点它或境界页都会引导去选；没血统不能渡劫
- 境界页、点击小阶的提示、血统页的境界链，全部按当前血统取名（不再所有人都写"炼气/筑基"）

### 三、界面改纯文字（照参考图重做首页）

参考图是纯文字放置游戏：没有图标，全靠**一行行文字**认路。我们照这个语法重排：

- **状态区**改成参考图那种 `【境界】血奴初期 第 1/36 阶`、`【等级】Lv.1`、`【轮回】0 世` 的文字行
- **挂机块从一块大画面压成三行字**：`【挂机】◈10.5/分…` / `【已挂】2秒 【待领】◈0` / `【分工】…` + 两个按钮
- **首页入口改成纯文字方块菜单**，按「今天 / 养成 / 其他」三组一次摊平（招募 / 兑换 / 任务 / 悬赏 / 游历 / 成就 /
  血统 / 境界 / 评级 / 秘术 / 权限 / 基地 / 基因锁 / 伴生体 / 转生 / 图鉴 / 分工 / 指南 / 设置）——
  上一版要跑"首页 + 成长子页 + 顶栏"三个地方才找得到，这次一屏找完
- **没解锁的不再铺一片灰格子**（一眼全是"未解锁"等于没信息），收成一行小字："还没解锁：… （推图会一个个亮起来）"
- 底部导航去掉图标，只留文字；顶栏的 ❓/⚙ 改成「指南」「设置」文字按钮
- 删掉 `.stage`（大主视觉）/ `.stamp-grid` / `.today-strip` 三套已经没人用的样式

### 四、同源修正

- 玩法指南：第 ① 章改写副本打法（连打波次、血量继承、随机遭遇不拦路），第 ⑤ 章改写血统时机，
  新增第 ⑮ 章「血统与境界线」，第 ⑭ 章的入口说法改成实际的首页文字菜单
- 新手欢迎语、老手引导（coachmark）里的"Lv.10 觉醒血统""选前进路线"等旧说法一并改掉
- 等级那一格不再叫「修为」（就是等级），避免和"修为什么时候涨"混淆

### 测试

`test_game.js` 316 → **326 项**（新增波次规划、血统境界线共 10 项），`test_ui.js` 83 → **86 项**
（新增"首页纯文字""入口分三组摊开""没解锁收成一行"等断言）。

## V8.0 · 2026-09-14 · 按对标产品的骨架补三套机制 + 首页改成「主视觉 + 印章入口带」

父亲大人说"以它的东西为基础"。这一次没有再调样式，而是把包里的数据表翻出来**逐条数**：
角色 122、技能 118、灵根 173、秘境 1100 层、宗门等级 321 级、敌队 539、游历事件 601、
装备 169、秘术 41 条、阵型 28、副本 29、洞府 21、丹方 9、弟子打工 14。

对照下来，我们**已经有**的（只是名字不同、机制一致，不动它）：血清=炼丹（材料+点数→永久强化）、
阵型=阵法、主神权限=洞府、挂机分工=弟子打工、无限回廊=秘境、境界渡劫=境界、套装=套装。
**真的缺**的是三条长线加一处主界面结构，这次全部补上。

### 新增 1：主神评级（对标「宗门等级」321 级）

它那条线是**随进度自动涨**的，321 级，每级抬全队属性。我们做成一样的：

- 通关普通 / 困难 / 地狱首通 +12 / +26 / +48（重刷减半）、每赢一场 +2、挂机每分钟 +1.2
- 每升 1 级：全队（含主角）全属性 **+0.5%**，永久生效、转生保留，共 120 级
- **不用手动点**——这是"推图顺便变强"的那条线，首页印章入口带里能看

### 新增 2：秘术阁（对标「KeJi」41 条线）

收成 12 条主轴，每条每级 +0.2%~0.5%，合 550 级：

- 前 8 条加战斗：攻伐诀 / 体修术 / 护法咒 / 身法诀 / 会心术 / 破魔经 / 灵法诀 / 御风术
- 后 4 条加挂机经济：采气术（产出）/ 悟性诀（经验）/ 聚运术（掉落）/ 静心诀（离线效率）
- 升级**只花 ◆异界结晶**——给高级货币开第二条长线出口（抽卡之外的钱终于有地方放）
- 加成分别并进「属性百分比区」与挂机产出 / 经验 / 掉落 / 离线效率四处

### 新增 3：游历奇遇（对标「YouLi」601 条）

挂机不只是涨数字。现在**每累计 10 分钟**（在线、离线都算）路上会冒一次随机奇遇，共 12 种：
灵草偶得 / 灵泉洗髓 / 残卷觅迹 / 秘境裂隙 / 妖兽伏击 / 前辈指点 / 晶石矿脉 / 遗落行囊 /
心魔考验 / 兽魂残响 / 天降机缘 / 同道馈赠。
攒满会挂在首页游历条上，**不会过期丢东西**，回来点一下就行。

### 首页改成「主视觉 + 两枚匾额 + 印章入口带」

对标产品的主界面是：中间一块画面、底部一排图形化入口、二级内容各占一页。照这个骨架重排：

1. **主视觉区**（`.stage`）：金色描边 + 一圈极淡星尘当"画面"，最大的数字居中；全页只有这一块用金色描边。
2. **两枚匾额**：主线（第几步 / 任务名 / 去完成）与今日（每日任务 / 免费招募 / 悬赏倒计时）并成一排，
   替掉原来"一整张主线卡 + 一条今日条"的竖排，首页少掉近半屏。
3. **印章入口带**（`.stamp-grid`）：9 格图形入口（招募 / 主神评级 / 秘术阁 / 兑换 / 任务 / 悬赏 / 游历 / 图鉴 / 成就），
   右上角小角标显示等级和待领数量；旧的 `.feat-grid` 宫格与 `featureBtn()` 一并下线。
4. **游历条**：没有待领时显示"距下一次多久"，有奇遇时变金。

### 同源修正（说明跟着实装走）

- 玩法指南新增第 ⑭ 章「主神评级 · 秘术阁 · 游历奇遇」，数值从 `DATA` 直接派生，不另写一套文案。
- 所有写「首页功能宫格」「今日横条」的旧说法，改成实际的「印章入口带」「今日那块」。
- `.kv` 键值行在样式表里原来写了两份（改一处漏一处），合并成一处；长标签可收缩、数值不换行。
- 顺手修掉秘术阁说明里漏出来的 markdown 星号。

### 测试

`test_game.js` 298 → **316 项**（新增评级 / 秘术 / 游历 18 项断言），`test_ui.js` 76 → **83 项**。

## V7.3 · 2026-09-14 · 二级内容独立成页（对标产品的骨架）

父亲大人要「先照它的东西来」。把包里的界面图集再对一遍，它最明显的一条结构不是配色、不是图标，
而是：**点系统入口 = 换一页**，不是从底部顶上来一叠抽屉。我们原来是后者——每次开系统都有一层遮罩
从下面滑上来，关掉再滑下去，页页如此。

### 改了什么

1. **系统面板全部改成独立整页**：占满一屏、顶部一条匾额式标题栏（左侧返回键 + 居中标题 + 金色短横），
   底部导航被整页盖住，不会再出现「半屏抽屉 + 后面露着首页」的两层结构。
2. **只留一种弹窗**：`确认/提示/事件选择`这类必须打断的，仍然居中弹；其余一律整页。
   随机事件的选项框也收进居中弹窗——它带返回键的话，玩家可以直接返回跳过事件。
3. **嵌套页面变成"页压页"**：例如「玩法指南 → 货币图鉴」，现在是真的一层压一层，返回键逐层退，
   观感与操作都跟对标一致。
4. 原来的 `showPanel / updateModal / 滚动保持 / 原地刷新（不闪屏）` 全部照旧可用，
   整页与居中弹窗共用同一个内容容器，所以「开箱、加点、买东西原地刷新」这些优化没有回退。

### 顺带修的

- `updateModal` 原来只会去找居中弹窗的标题节点，整页形态下会取空。现在两种形态都认。
- 版本号 `V7.2 → V7.3`。

## V7.2 · 2026-09-14 · 首页瘦身（主视觉 + 入口带）

父亲大人指出「几乎把所有东西都塞到主页里了」。回头把包里的 `MapList` 图集放大细看，
对标产品的主界面其实很简单：**主体是一块世界画面，底部一排图形化入口**（水滴形印章：游历 /
坊市 / 物品 / 活动 / 副本 / 宗斗 / 出战 / 收徒 / 祭坛…），二级内容全部在弹窗里，主界面不堆卡片。
我们原来是「5 张卡 + 14 格宫格」竖着堆。

### 首页现在只有四块

1. **顶部状态区** —— 境界 / 修为 / 轮回
2. **挂机主卡** —— 全页唯一主视觉（金色描边 + 最大字号）
3. **「今日」横条** —— 原来的四行今日卡收成一条，副标题一行给完「每日任务 / 免费招募 / 悬赏剩余时间」，
   有可领奖励时整条变金；点开才是四行详情
4. **主线提示条 + 六个入口**（招募 / 兑换大厅 / 任务 / 限时悬赏 / 轮回图鉴 / 成就）

一屏就能装下，不用往下滚。

### 养成线搬到「轮回者 → 🌱 成长」

基地建设 / 主神权限 / 境界渡劫 / 基因锁 / 伴生体 / 转生天赋 —— 六条长期线集中在一个子页，
每条显示"现在到哪了"（如「境界：炼气初期（第 3/36 阶）」「权限 Lv.2 / 10」），
点进去才是完整面板；未解锁的直接显示解锁条件。首页只留一行指引。

### 说明与实装同步

玩法指南、新手欢迎语里凡是写「首页今日卡」的地方，一并改成「今日横条」——
说明跟着实装走，不留旧说法。

### 测试

- 逻辑 **298 项** / 界面 **76 项**，全绿。
- 界面测试新增「首页指向成长子页」「伴生体 / 主神权限在成长子页」；
  原「首页有伴生体入口」「首页有主神权限入口」两条按新信息架构改写（旧断言绑的是旧 IA，属用例过时）。


## V7.1 · 2026-09-14 · 界面系统重构（统一"匾额"结构）

父亲大人反馈"界面太乱"。根因不是元素多，是**没有统一的规则**：359 处散落的内联
`font-size / color / margin`、四五种按钮长相、卡片圆角 12/14/18 混用、颜色当装饰用。

这一版按对标产品的界面语法重做：**近黑面板 + 一道细描边 + 标题带竖条**，颜色只留含义（金=资源/关键、
红=主动作、绿=完成），尺寸全部走令牌。

### 设计系统（`css/style.css` 顶部）

- **令牌**：间距 4/8/12/16/22 五档；字号 10/11/12/13/15/17 六档；圆角收敛成 10 / 7 两种。
- **面板**：全站卡片统一成"近黑底 + 1px 描边 + 顶部 1px 内高光"，标题前统一加**金色竖条**。
  改这一处，首页 / 副本 / 角色 / 背包 / 所有弹窗同时生效。
- **按钮**：收敛为三级 —— primary（实心红·主动作）、gold（实心金·资源相关）、默认/ghost（描边·次要）。
- **文字层级**：新增 `.lead / .note / .hint` 三档正文与 `.gold / .green / .red / .dim` 含义色，
  替代散落的内联写法定值；`.mb2 / .mt3` 等间距工具类同理。
- **弹窗**：标题栏与面板标题同一套（左侧金色竖条 + 右侧方形关闭键），不再是"弹窗一套、卡片一套"。

### 首页信息架构重排

- **删掉底部那行 8 个 `·` 分隔的小字入口**——它和上面的宫格重复（图鉴 / 悬赏 / 境界 / 主神权限都出现两次），
  是"乱"最直观的来源。设置与玩法指南上收到**顶栏右上角两个图标**。
- 功能入口改成**三列宫格 + 分组**：养成（基地建设 / 主神权限 / 境界渡劫 / 基因锁 / 转生 / 伴生体）、
  收集（招募 / 兑换大厅 / 轮回图鉴 / 成就）、目标（任务 / 限时悬赏 / 道具背包 / 货币图鉴）。
- 「个人房间」从"一整张卡 + 3 行 + 进度条"收成**一行**（经验条本来就在顶部状态区里，重复了）。
- 底部导航选中态改成金色 + 顶部指示条，未选中图标降饱和，视线不再被四个彩色图标平分。

### 机械替换

- 把最高频的 103 处内联样式改写成统一类（间距 60 处、文字层级 43 处）；
  替换脚本**跳过含模板表达式的标签**，不会误伤 `${}` 里的尖括号。
- 删掉已无引用的 `.more-row` / `.link-btn` 样式，避免留下死类把后续改动带偏。

### 测试

- 逻辑 **298 项** / 界面 **75 项**，全部通过。并用本地预览逐页目视确认：首页、轮回副本、招募、概率公示、
  主神权限、队伍阵型、境界渡劫。


## V7.0 · 2026-09-14 · 世界观移植（对标《道友修仙》实包）

这一版的做法和以往不同：**拿到了《道友修仙》的安装包（com.gaoyu.xiuxian 11.6），把它 170 张数据表与
5080 条界面文案读了一遍**，挑出对我们真正有用的机制与界面写法，搬进《无限轮回》的世界观里。
只搬**机制与结构**，不搬它的名称、美术与素材（沿用主神空间 / 恐怖副本的世界观与自己的视觉）。

### ★ 招募券（对标它的"基础招徒卷 / 高级招徒卷"）

**它的做法**：招募不是直接花货币，而是花**券**；券能买、也能从玩法里掉（商店里
「高级招徒卷 150 灵石 / 基础招徒卷 50万金钱」）。所以"打副本 → 掉券 → 去抽"本身就是一条循环。

**我们的落地**：三个池子各配一张券 —— 🎫轮回招募券（普通）/ 🎫圣契招募令（高级）/ 🎫异界征召令（限定）。

- 抽卡规则只有一条：**有券先用券，券不够才扣货币**。
- 十连要么给 10 张券、要么给足货币，**不混着扣**（避免"扣了 3 张券又扣一半钱"这种说不清的账）。
- 券的来源铺满各个玩法：普通战 18% 掉普通券、精英 28% / Boss 50% 掉高级券、地狱 Boss 35% 掉限定券；
  每日全清、每周全清、周常、登录七天、限时悬赏、四个商店都能拿。
- 背包里的券有「去招募」快捷键；道具详情里写明"在哪用 / 怎么扣"。

### ★ 概率公示（对标它在招募界面直接写概率）

**它的做法**：招募界面直接写「招募到 37% 血脉修士的概率为 5%，25% 血脉的概率为 15%…」——不藏。

**我们的落地**：招募页底部新增「📊 招募概率公示」，逐池列出：

- 每一档的真实出率（和 `D.RECRUIT_POOLS.rates` 同一份数据，抽卡就用它，不存在"写着好看"）；
- 单抽 / 十连分别花什么（货币多少、或券多少张）；
- 保底规则原文 + **还差几抽触发保底**（SSR / UR / UP 分开算）。

### ★ 境界细化成 36 小阶（对标它的 37 阶境界）

**它的做法**：境界写成「凡体 → 练气初/中/后/大圆满 → 筑基初/中/后/大圆满 → …」，一条线 37 格。

**我们的落地**：原来是「每 10 级一个大境界，共 10 境」，两次突破之间隔 10 级，练级路上长期没有任何反馈。
现在改成 **9 大境 × 初/中/后/大圆满 = 36 小阶**（炼气初期 → 渡劫大圆满）：

- 单阶加成从 +5% 降到 **+1.4%**，36 阶合计 **+50.4%**，总量与旧的 10 境 × 5% 对齐；
- 但"变强的出口"从 10 个变成 36 个，约每 2.6 级就能破一次；
- 累计消耗也对齐旧表（点数 389.0 万 vs 旧 387.8 万，材料 502 vs 旧 504）；
- 材料档位跟着等级走，5 种强化材料全部用上（不再只有前两档）；
- **老存档自动迁移**：旧第 N 境 = 新第 4N 阶，加成总量不变，用 `realmScaled` 标记只转一次；
- 境界面板改成「大境界一行、行内四个小阶」，一眼看到走到哪一格、下一格要什么。

### ★ 主神权限：高级货币的长线出口（对标它的"洞府"）

**它的做法**：洞府 21 级，一次性把高级货币（钻石）投进去，永久抬高挂机倍率、任务数、副本次数 ——
高级货币不只有"抽卡"一个出口。

**我们的落地**：新增「🔑 主神权限」10 级，花 ✦圣洁晶石 + ◆异界结晶（累计 3797 ✦ / 2530 ◆）：

| 等级 | 效果 |
|---|---|
| 1 / 4 / 9 | 挂机产出 +6%（累计 +18%） |
| 1 / 6 | 挂机经验 +4%（累计 +8%） |
| 2 / 7 | 离线上限 +0.6 小时（累计 +1.2h） |
| 3 / 8 | 每日扫荡次数 +4（累计 +8） |
| 5 / 10 | 离线效率 +5%（累计 +10%） |
| 10 | 额外全队全属性 +5% |

永久生效、**转生不清空**（它是"主神对自己的授权"，不是角色属性）。入口在首页宫格与顶部状态区的「轮回」格。

### ★ 阵型：具名组合（对标它的 28 种"阵法"）

**它的做法**：不笼统说"同门派有加成"，而是具名阵法 + 具体人数（如「3 个巨剑门 + 3 个无极门」）。

**我们的落地**：阵营羁绊升级为 6 种具名阵型，队伍页直接显示"你站的是哪一阵、还差什么"：

| 阵型 | 要求 | 加成 |
|---|---|---|
| 双子阵 | 同一阵营 2 人 | 攻 +3% |
| 三才阵 | 同一阵营 3 人 | 攻/命 +6% |
| 四象阵 | 同一阵营 4 人 | 攻/命 +10%、技 +5% |
| 五行归元阵 | 同一阵营 5 人（主角可补位） | 攻/命 +14%、技 +8% |
| 双柱阵 | 两个阵营各 2 人 | 攻/命 +4% |
| 四海阵 | 四个阵营各 1 人 | 攻/命/技 +4% |

- 新规则：**主角是"万能补位"** —— 他不属于任何阵营，但编阵时可以顶任意一个阵营的名额，
  所以 4 个同阵营 + 主角能凑成 5 人五行归元阵；
- 「同阵营」那一族只取命中的最高档，不会 2/3/4/5 人重复叠；双柱阵与四海阵是另外两条独立路线。

### ★ 界面调整

- 首页顶部新增**状态区**：『境界』『修为』『轮回』三栏（对标它主界面最上面的『境界』『修为』『修龄』）。
- 首页功能宫格补上「🔑 主神权限」「📕 图鉴」（10 格，两列排满）。
- 招募页每池新增**券状态行**：显示券的张数与"有券先用券 / 没券会花什么"。
- 奖励文案统一走一个出口（`rewardText`），道具类奖励（券、箱）现在在任务 / 悬赏 / 登录里都能正确显示。
- 玩法指南新增第 ⑬ 章「主神权限」，并改写了阵型、货币、招募券、36 阶境界相关章节。

### 修复

- 「概率公示」页里 SR/SSR/UR 出率挤在一行没有间隔（`.rate-row` / `.pity-row` 原来只在卡池卡内生效）。
- 登录第七天的 SSR 自选券会吞掉当天的道具奖励（`applyRewardObj` 统一处理 `ssrTicket` 与 `item`）。

### 测试

- 逻辑 **298 项**（原 244 + 新增 54：券的扣费优先级、十连整付规则、概率公示文案与数据一致、主神权限 10 级效果与门槛、六种阵型判定与主角补位、境界 36 阶与迁移）
- 界面 **75 项**（原 63 + 新增 12：状态区 / 主神权限 / 概率公示 / 阵型 / 券入口 / 券详情）
- 全部通过。另外用本地预览逐屏目视确认过：首页状态区、招募页券行、概率公示、主神权限、队伍阵型、36 阶境界。


## V6.0 · 2026-09-14 · 整体重构

这一版把「UI 与交互优化方案」的批次 1–6 全部落地，并按对标产品的机制结构**重做了招募经济**。
原则：每一处改动都能在代码里找到，每一项说明都有对应实装。

### ★ 招募系统重做

**问题**：高级池与限定池同价（✦100），而限定池每项出率都更高 —— 高级池被全面碾压，
等于"一个池子 + 一个没人点的按钮"。

**现在：三池花三种货币、出三种结构**

| 池子 | 货币 | 出率结构 | 定位 | 保底 |
|---|---|---|---|---|
| 普通招募 | ◈点数 5000 | N/R/SR（**不出 SSR/UR**） | 攒碎片升星 | 无 |
| 高级招募 | ✦圣洁晶石 100 | **SR 起抽**，SSR 25% / UR 3% | 补图鉴，**优先给未拥有的角色** | SSR 50 / UR 100 |
| 限定招募 | ◆异界结晶 60 | SR 62% / SSR 33% / UR 5%，**只出当期阵营** | 定向拿当期 UP | SSR 50 / UR 100 / **UP 50** |

- 当期 UP 按自然周从 SSR 池轮换，不写死角色。
- 限定池：SSR 里 50% 直接是当期 UP；50 抽内必出当期 UP。
- 保底改为**按池分开记账**（`S.recruit.pity`），老存档的旧计数字段自动迁移，进度不丢。
- 池子卡片上直接写明货币 / 出率 / 保底进度 `n/50` / 本期 UP 是谁。

### ★ 三套新玩法

**挂机分工**（首页 →「🧭 挂机分工」）

- 4 条产线各派 1 名领队：闭关修炼（经验）/ 灵材采集（强化材料）/ 外围探索（点数）/ 主神守卫（异界结晶）。
- 领队加成 = 战力 / 30000，封顶 +150%；**没派领队就不产出**。
- **上阵主力不能派、同一人不能管两条线** —— 这是板凳角色的用处。
- 产线收益与挂机收益同池累计，离线期间照跑（离线结算也发材料）。

**限时悬赏**（首页 →「🔥 限时悬赏」）

- 4 条目标各带自己的截止时间（72/96/120/168 小时），**到点作废**。
- 奖励是硬通货（圣洁晶石 / 异界结晶 / 血统结晶）；全部结束后可**开新一期**。
- 首页「今日」卡显示最快到期那条的剩余时间和"几条可领"。

**境界 · 渡劫**（首页「更多」行 或 主角详情）

- 主角每 10 级一境，共 10 境（炼气 → 飞升）。
- 成功：全属性**永久 +5%**（进 `effectivePlayerStats`）；失败：只扣材料与点数，**等级不掉**，可反复挑战。
- 成功率 95% → 50% 递减，界面上逐项标出等级 / 材料 / 点数够不够。

**伴生体 + 五行克制**（首页 →「🐾 伴生体」）

- 第二条养成线（对标灵兽驯宠）：上阵 1 只，给**全队**加属性（主角也吃）+ 一个被动 + 五行克制。
- 孵化花「兽魂石」：副本 Boss 必掉 1~3 颗、精英 30% 掉 1 颗、主神商店（通关 W03）可买。
  10 颗孵 1 只，稀有度 N 50% / R 30% / SR 17% / SSR 3%，共 12 只。
- 重复孵到同一只 → 转**兽魂**；兽魂升阶，每阶在基础加成上再 +15%，满级 Lv.10。
- **五行克制**（金木水火土，金克木、木克土、土克水、水克火、火克金）：每个世界有自己的属性，
  带对克制的伴生体进本全队伤害 **+15%**，带反了 **-8%**。克制在**战斗引擎的伤害公式里**结算，不是界面文案。

### ★ UI 与交互

- **底部导航 5 → 4**：`主神空间 / 轮回副本 / 轮回者 / 背包`；
  「队伍 / 角色 / 装备」合并进「轮回者」子页（顶部胶囊切换，各自记住滚动位置）；
  旧页签名做别名映射，历史跳转与引导高亮一行未改。
- **背包升为一级页签**，卡片上直接给「开 / 喂 / 服 N」快捷键。
- **首页「今日」卡**：挂机 / 每日任务 / 免费招募 / 限时悬赏四行 +「一键收取」
  （收挂机、任务、周常、成就、图鉴里所有已达成未领取的奖励；用前后账户快照求差，说明与实装不可能不一致）。
- **首页主界面重排**：顶部是**挂机大字**（◈/分 的大号数字 + 已累积 / 待领取 + 一键收取 + 挂机分工），
  下面是**图标宫格**（图标在上、名字在下，8 格）—— 放置类主界面的通用做法，一眼扫得到功能。
- **推图连续性**：结算页「↻ 再来一次 / › 下一关」，最后一关自动顺延下一难度、下一世界。
- **角色页筛选 / 排序 / 搜索**：排序含「未满级优先」「没穿装备优先」；搜索只重画网格，输入框不失焦。
- **五条全局交互规则**：热区 ≥44px、300ms 防连点、货币收支 ±数值跳动（`Core.setCurListener` 广播）、
  成功/失败回执（失败抖动）、大额消费二次确认（≥1000，可在设置里关）。
  *有意的偏差：单抽不加确认（连续动作，加了伤手感）；十连与商店大额购买都有确认。*

### 数值口径（新增部分）

| 项 | 取值 | 依据 |
|---|---|---|
| 限定池单抽 | ◆60 | 比高级池便宜，门槛是"只出当期阵营" |
| 挂机材料产出 | 0.08/分钟（约 5 个/小时） | 对齐商店 30◈/个，约等于挂机点数的 20% |
| 领队加成 | 六维值/260，封顶 +150% | 前期线性、后期封顶 |
| 产线加成依据 | 领队的**对应六维**（闭关=精神 / 采集=肌肉 / 探索=神经 / 守卫=免疫），维值/260 封顶 +150% | 让"练谁、派谁"变成真的取舍，而不是谁战力高谁上 |
| 境界加成 | 每境 +5%，共 10 境 | 满境 +50%，与基因锁量级相当 |
| 悬赏奖励 | 400–1200 ✦ | 约等于首通奖励量级，是"顺手多拿一份" |
| 兽魂石产出 | Boss 必掉 1~3、精英 30%、商店 4000◈/5 颗 | 约 3~5 个 Boss 能孵一只 |

### 验证

- `node scripts/test_game.js` → **244 项全绿**
- `node scripts/test_ui.js` → **63 项全绿**
- 存档向后兼容：老档自动补 `serums / idle.lines / bounty / beast / player.realm`，
  招募保底计数从旧字段迁移，悬赏在缺 `list` 时按当前进度补生成。

### 已全部完成的清单

本版把原计划里"留到下一版"的三项也做完了，没有遗留：

1. **伴生体 / 五行克制** —— 第二条养成线，已实装（见上）。
2. **六维属性对挂机产线的细分影响** —— 四条产线各看一维，已实装。
3. **悬赏按进度动态生成** —— 目标跟着当前世界、等级、强化次数、图鉴/回廊/伴生体/境界走，每期重算，已实装。
